# Knowledge Graph Kit - Python Dependencies

# Core functionality
pyyaml>=6.0

# Gemini AI chat integration (built-in feature, needs API key)
google-generativeai>=0.3.0

# Optional: AI chat API server (templates/*/gemini_api_server.py)
# Only needed if you run the Gemini chat server alongside the viewer:
# flask>=3.0
# flask-cors>=4.0

# Everything else in the core toolkit uses the Python stdlib:
# - http.server (for web server)
# - json (for data storage)
# - pathlib (for file handling)
# - datetime (for timestamps)
